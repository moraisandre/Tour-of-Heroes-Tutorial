#.travis.yml

language: node_js
node_js:
  - "8.2.1"

branches:
  only:
    - master

before_script:
  - rm -rf package-lock.json node_modules && npm install
  - npm install -g @angular/cli

script:
  - ng build --env=prod